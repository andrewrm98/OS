/*
 * Andrew Morrison
 * Peter Christakos
 * Project4 Manager.c Source Code
 */

#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <unistd.h>

// global guys
char memory[64]; //memory

/*
 * "map takes the value and determines if a
 * to complete said instructions." - grandmaster coache
 */
int map (int process, char* instruction, int address, int value)
{
	return 0;

}

/*
 * "store takes the value and stores it into the physical address specified
 * to complete said instructions." - grandmaster coache
 * 1 - check valid bit
 * 2 - check write permission 
 * 3 - store bit
 */
int store (int process, char* instruction, int address, int value)
{
	return 0;
}


/*
 * "load takes the value and stores it into the physical address specified
 * to complete said instructions." - grandmaster coache
 */
int load (int process, char* instruction, int address, int value)
{
	return 0;
}

int main(int argc, char **argv)
{
	return 1;
}